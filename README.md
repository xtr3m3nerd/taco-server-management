# taco-server-management

```
bash <(curl -s https://raw.githubusercontent.com/xtr3m3nerd/taco-server-management/main/install-redirect-fqdn.sh)
vim redirect-fqdn.sh
systemctl start redirect-fqdn.service
systemctl enable redirect-fqdn.service
bash <(curl -s https://pterodactyl-installer.se)
# RUN NODE Configure
systemctl start wings
systemctl enable wings
```
